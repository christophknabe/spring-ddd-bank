#Application scpecific configuration for usage by Spring for running the test suite. Knabe 2017-09-25
#Only differences to src/main/resources/application.properties have to be noted here.

#For test scenario use a memory-based Derby database, create it:
spring.datasource.url=jdbc:derby:memory:derby-db;create=true